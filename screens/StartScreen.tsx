import React from "react";
import { StyleSheet, Text, View } from "react-native";

const StartScreen = () => {
  return (
    <View>
      <Text style={{ color: "#fff" }}>StartScreen StartScreen</Text>
    </View>
  );
};

export default StartScreen;

const styles = StyleSheet.create({});
